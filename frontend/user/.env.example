# Use full API URL if not using Vite proxy (also used to resolve /uploads/... image paths in production)
VITE_API_URL=http://localhost:5000/api
# Socket server (no /api)
VITE_SOCKET_URL=http://localhost:5000
