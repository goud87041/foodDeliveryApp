# Local dev (`npm run dev`): leave empty — Vite proxies /api → http://localhost:5000
#
# Production (`npm run build`): put URLs in .env.production or your host's env vars:
# VITE_API_URL=https://your-api.onrender.com/api
# VITE_SOCKET_URL=https://your-api.onrender.com